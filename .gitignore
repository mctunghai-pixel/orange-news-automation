*.json
!translated_posts.json
!market_data.json
!mse_data.json
!archive/
!archive/*.json
!youtube_data.json
!deny_list_videos.json
*.log
assets/generated/
__pycache__/
.env
files.zip
logs/*
# Allowlist: ig_runner.py state file must be committed back across hourly runs
# for idempotency. Per-run heartbeat (logs/ig_publish_run_status.json) and
# per-day publish logs stay ignored.
!logs/ig_publish_state.json

# Allowlist: telegram_poster.py state file (same idempotency reason as ig_runner)
!logs/telegram_publish_state.json

# macOS system files
.DS_Store
**/.DS_Store

# Documentation folders (local only)
Claude Conversations/
Chat conversations/

# Backup folders and files
backups/
*.backup
*.bak
*.save

# Old workflow backups
.github/workflows/*.backup

# GMBA thesis materials (personal academic work — local only)
/scripts/thesis_capture/
/figures/
